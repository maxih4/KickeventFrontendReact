# Vite reads these values while building the frontend. Do not add a trailing slash
# to VITE_BACKEND_URL because the app appends API paths itself.
VITE_BACKEND_URL=http://localhost:8080
VITE_GOOGLE_MAPS_API_KEY=
